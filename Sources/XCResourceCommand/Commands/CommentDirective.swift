import Foundation

enum CommentDirective {
    static let useRaw: String = "xcresource:use-raw"
    static let key2formExclude: String = "xcresource:key2form:exclude"
    static let targetStringsdict: String = "xcresource:target:stringsdict"
}
